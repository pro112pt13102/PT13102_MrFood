<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.peter.project1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".ChiTietActivity" />
        <activity android:name=".GioHangActivity" />
        <activity android:name=".ThongTinActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SanPhamActivity" />
        <activity android:name=".GioiThieuActivity" />
        <activity android:name=".LienHeActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".Service.OnClearFromRecentService"
            android:stopWithTask="false" />

        <activity android:name=".Service.MuaNgayActivity" />
        <activity android:name=".MuaNgayActivity"></activity>
    </application>

</manifest>